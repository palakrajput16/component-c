# Component C - Week 1

## Submission Information

**Component:** C  
**Group Number:** __________  
**Week:** 1  
**Course:** CSIT332 - Principles of Machine Learning

### Contributors

- Palak Rajput
- Sinchana Naik
- Rouchelle Philip

---

## Overview

This repository contains the Week 1 work for Component C.

The implementation covers the four required statistics:

1. Winner-correct rate
2. Kendall's tau
3. Optimism
4. Power using a paired t-test for c1 vs c2

The analysis is performed for:

- `n_sub = 100`
- `n_sub = 500`

The repository also contains the supplied toy data, the extended generated results, the code used to generate the extended results, and the written hand calculations.

---

## Folder Structure

```text
Component_C/
│
├── README.md
│
└── Week_1/
    │
    ├── data/
    │   ├── truth.csv
    │   ├── results.csv
    │   └── results_extended.csv
    │
    ├── docs/
    │   ├── Week_1_Hand_Calculations.pdf
    │   └── Edge_Cases.pdf
    │
    └── src/
        ├── generate_results.py
        └── compute_statistics.py
```

`Edge_Cases.pdf` should be added once the edge-case document is completed.

---

# How to Run the Code

## Requirements

Python 3 is required.

The statistics code uses:

- pandas
- scipy
- numpy

If these packages are not already installed, run:

```bash
pip install pandas scipy numpy
```

---

## Step 1: Generate the Extended Results

The supplied toy data is already present in:

```text
Week_1/data/results.csv
Week_1/data/truth.csv
```

To generate the extended results, open a terminal in the `Week_1` folder and run:

```bash
python src/generate_results.py
```

This generates:

```text
Week_1/data/results_extended.csv
```

The generated file contains:

- 20 replicates at `n_sub = 100`
- 20 replicates at `n_sub = 500`
- 3 classifiers
- 5 folds per classifier per replicate
- 600 total rows

The supplied replicate 1 at `n_sub = 100` is preserved, and the remaining results are generated by the script.

---

## Step 2: Compute the Four Statistics

After `results_extended.csv` has been generated, run:

```bash
python src/compute_statistics.py
```

The script reads:

```text
Week_1/data/truth.csv
Week_1/data/results_extended.csv
```

and prints the results for both sample sizes.

---

# Expected Output

The current results produced by the code are:

```text
True best classifier: c1 (true_auc = 0.85)

--- n_sub = 100 ---
  n_replicates_used: 20
  n_replicates_dropped: 0
  dropped_replicate_ids: []
  tie_count: 0
  winner_correct_rate: 0.9
  mean_kendall_tau: 0.9333333333333333
  mean_optimism: 0.00408100000000004
  power_c1_vs_c2: 0.3
  sign_error_rate: 0.0

--- n_sub = 500 ---
  n_replicates_used: 20
  n_replicates_dropped: 0
  dropped_replicate_ids: []
  tie_count: 0
  winner_correct_rate: 1.0
  mean_kendall_tau: 1.0
  mean_optimism: -0.0018839999999999523
  power_c1_vs_c2: 0.85
  sign_error_rate: 0.0
```

### Summary

| Statistic | n_sub = 100 | n_sub = 500 |
|:--|--:|--:|
| Winner-correct rate | 0.90 | 1.00 |
| Mean Kendall's tau | 0.9333 | 1.0000 |
| Mean optimism | 0.004081 | -0.001884 |
| Power, c1 vs c2 | 0.30 | 0.85 |
| Sign-error rate, c1 vs c2 | 0.00 | 0.00 |

---

# What Each File Does

### `truth.csv`

Contains the true AUC for each classifier.

| Classifier | True AUC |
|:--:|--:|
| c1 | 0.85 |
| c2 | 0.82 |
| c3 | 0.76 |

Therefore, c1 is the true best classifier.

### `results.csv`

Contains the supplied toy results for replicate 1 at `n_sub = 100`.

### `generate_results.py`

Generates the extended results dataset used for the Week 1 analysis.

### `results_extended.csv`

Contains the complete 600-row dataset used by the statistics code.

### `compute_statistics.py`

Computes:

- winner-correct rate
- mean Kendall's tau
- mean optimism
- power for c1 vs c2
- sign-error rate

for both `n_sub = 100` and `n_sub = 500`.

### `Week_1_Hand_Calculations.pdf`

Contains the manual calculations for the required statistics and verification against the Python implementation.

### `Edge_Cases.pdf`

Contains the decisions and rules for handling edge cases. This document will be added after the edge-case work is finalized.

---

# Reproducing the Results

To reproduce the complete Week 1 analysis from the repository:

```bash
cd Week_1
python src/generate_results.py
python src/compute_statistics.py
```

The first command creates or refreshes:

```text
data/results_extended.csv
```

The second command calculates and prints the statistics from that file.

The results should match the values reported in the written work.

---

# Data Format

The results data follows the required structure:

```text
dataset_id
protocol_id
n_sub
replicate_id
classifier_id
fold_id
auc
```

The truth data contains:

```text
dataset_id
classifier_id
true_auc
```

Each replicate contains 3 classifiers and 5 folds per classifier.

---

# Week 1 Deliverables

The Week 1 submission contains:

- Statistics code
- Data-generation code
- `truth.csv`
- `results.csv`
- Extended `results_extended.csv`
- Hand-calculation PDF
- Edge-case PDF

All files are contained within the `component-c` repository.

---

# Notes

- The statistics are calculated separately for `n_sub = 100` and `n_sub = 500`.
- The paired t-test for power compares c1 and c2 using matching `fold_id` values.
- The hand calculations use the supplied replicate 1 data at `n_sub = 100`.
- The Python implementation has been checked against the hand calculations.
- Edge-case decisions are documented separately in `Edge_Cases.pdf`.
